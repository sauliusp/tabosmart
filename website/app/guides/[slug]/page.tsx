import type { Metadata } from 'next';
import { notFound } from 'next/navigation';
import guides from '../../../content/guides.json';
export function generateStaticParams(){return guides.map(g=>({slug:g.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const g=guides.find(x=>x.slug===slug);return g?{title:g.title+' | Tabosmart',description:g.description,alternates:{canonical:'/guides/'+g.slug+'/'} }:{};}
export default async function Guide({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const g=guides.find(x=>x.slug===slug);if(!g)notFound();const schema={'@context':'https://schema.org','@type':'Article',headline:g.title,description:g.description,author:{'@type':'Person',name:'Saulius'},datePublished:'2026-09-08',dateModified:'2026-09-08',mainEntityOfPage:'https://tabosm.art/guides/'+g.slug+'/'};return <><header className="site-header"><a href="/" className="brand"><img src="/favicon.svg" width="42" height="42" alt=""/>Tabosmart.</a><a className="text-link" href="https://tabosmart.featurebase.app/">Help shape it ↗</a></header><main className="article"><a href="/#guides" className="text-link">← All tab guides</a><p className="eyebrow">PRACTICAL TAB MANAGEMENT</p><h1>{g.title}</h1><p className="article-meta">By Saulius, maker of Tabosmart · Updated 8 September 2026</p><p className="answer">{g.answer}</p>{g.sections.map(([title,body])=><section key={title}><h2>{title}</h2><p>{body}</p></section>)}{g.slug==='local-ai-tab-organizer'&&<p>Source: <a href="https://developer.chrome.com/docs/ai/prompt-api">Chrome’s Prompt API documentation</a> and <a href="https://developer.chrome.com/docs/ai/get-started">current device requirements</a>.</p>}<aside className="article-cta"><h2>What would make this work better for you?</h2><p>Share a real experience or vote on an idea in the Tabosmart community.</p><a className="cta" href="https://tabosmart.featurebase.app/">Give feedback ↗</a></aside><h2>Keep exploring</h2><ul>{guides.filter(x=>x.slug!==g.slug).map(x=><li key={x.slug}><a href={'/guides/'+x.slug+'/'}>{x.title}</a></li>)}</ul><a href="/privacy/">Privacy and permissions</a></main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,'\u003c')}}/></>}
