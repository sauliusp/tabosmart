# Real first-run and model-download verification

Updated 8 September 2026 for Tabosmart 1.3.0 source. **The real run is blocked before model removal and has not been executed.** The user reports reloading Tabosmart. The earlier tool inventory listed its reopened workspace, but did not verify the loaded build or model state. The prior Browser use security rejection still prohibits access to the extension URL and alternate access routes. No new rejection is claimed, and no prohibited page-access attempt was repeated.

## Model reset: verified research and unresolved scope

Chrome manages Gemini Nano downloads and removal. Removing Tabosmart data does not remove this browser-managed model. Chrome's documentation describes automatic management and model inspection in `chrome://on-device-internals`. [Chrome model management](https://developer.chrome.com/docs/ai/understand-built-in-model-management).

Chromium source includes an **Uninstall** control in its internal model page. Its original implementation calls the global model component manager; newer UI also has a broker asset uninstall control. This establishes an internal testing route in Chromium source, not the existence, current state, or scope of that control in the user's running Chrome. No internal control has been operated. [Original uninstall implementation](https://chromium.googlesource.com/chromium/src/+/12f44c87f58df74e8f7698f2ff721e0a1d5a9ab1%5E%21/), [current model-page source](https://chromium.googlesource.com/chromium/src/+/HEAD/chrome/browser/resources/on_device_internals/model_status.html.ts).

The model infrastructure is shared across profiles in Chromium source. Therefore a new profile alone must not be assumed to isolate a reset. The running installation's exact affected products and profiles remain unverified. Any future reset must first establish both a permitted route to complete the extension test and the actual model scope. If shared user state would be affected beyond the authorized test, resolve that concrete scope before removal. A separate test browser data directory could isolate data only if verified and permitted; it does not override the extension-URL policy. [Shared model infrastructure](https://chromium.googlesource.com/chromium/src/+/HEAD/chrome/browser/optimization_guide/model_execution/optimization_guide_global_state.h).

Do not delete guessed browser directories, remove unrelated components, change experimental flags, create disk pressure, or change enterprise policy to simulate model absence. Verify genuine no-model availability after an approved supported reset; do not equate a failed request with missing model files.

## Prepared fixture pack

Run `node scripts/first-run-fixtures.mjs` to generate and validate the manifest using Node HTTP requests only. This has passed for all **64 fixture entries**. Run `node scripts/first-run-fixtures.mjs --serve` to serve the index at `http://127.0.0.1:8766/` when the real test becomes executable. Neither command opens or controls a browser or calls AI. The server is loopback-only and contains no external resources, cookies, telemetry, or persistent personal data.

[Fixture manifest](../qa/first-run-fixtures.json) maps every entry to intended test window A or B, URL, title and state. There are 32 entries per window:

| Fixture type                        | Per window | Intended evidence                                                                                         |
| ----------------------------------- | ---------: | --------------------------------------------------------------------------------------------------------- |
| Garden studio project               |          6 | Same-site grouping and editable names                                                                     |
| Baltic trip project on another host |          6 | Multiple simultaneous projects                                                                            |
| Cross-site title pairs              |          8 | Four pairs; isolate a pair to check its exact shared anchors without competing overlapping topic evidence |
| Exact duplicate draft               |          3 | Retained copy visible; independently edited synthetic drafts must not be assumed identical                |
| Query/fragment variants             |          3 | Distinct full URLs remain distinct                                                                        |
| Protected, pinned, audible, active  |          4 | Exclusions applied to fixture tabs only                                                                   |
| Existing group                      |          2 | Native group boundaries respected                                                                         |

A unique `TabosmartTxx QA` marker identifies each distinct fixture document without adding common title words that would bias grouping. Duplicate copies intentionally share a title and URL. All URLs contain the run ID. Audio is a quiet local tone that starts only when its native Play control is pressed. Draft fields contain disposable synthetic text only. Planned protected/pinned/grouped states are instructions for the eventual operator, not browser states already established.

## 1.3.0 startup, history and naming checks

A fresh installation automatically starts observation and history analysis after Chrome grants the required permissions. Verify that the nonblocking disclosure describes open-tab metadata and available history, without an optional-history request or mandatory onboarding step. Existing paused or erased state must stay paused. Do not erase personal state merely to imitate a fresh installation.

History analysis must show actual processed counts and distinguish partial, indexing/updating and completed coverage. A real test cannot claim lifetime history or access to deleted, Incognito or unavailable records. Verify exact-current-URL relevance and all valid suggestions, including more than five cards and more than eight older tabs. Review batches must remain at most 200 without losing members. History-off erases its derived hash index; pause retains checkpoints without processing. Keep these separate from existing exact observed-tab/shelf/recovery records.

Fresh/missing AI preferences default on; existing explicit and legacy off values stay off. A default does not create a model or prove readiness. Use the explicit setup control when Chrome needs a download. Automatic naming runs for eligible visible groups while enabled and ready, with deterministic output first, ten automatic attempts per interface, names before optional wording and stale-result guards. Open reviews and edits remain fixed.

Verify Close removes only its validated workspace and returns to browsing, including the final-tab replacement case. Verify toolbar core/history/AI work, completed counts, pause/error and Port cleanup. Chrome documents that a triggered model download continues after the initiating tab closes; the old page cannot keep reporting progress, and reopening must query availability. This is a separate closure scenario, not proof of completion. [Chrome model management](https://developer.chrome.com/docs/ai/understand-built-in-model-management#initial_model_download)

A future permitted check must record actual titles, URLs, windows and eligibility flags. Exercise unfamiliar subjects/domains, related tabs separated across 100+ tabs, mixed languages, generic wording, existing groups and dismissals. Do not expect brand-only publisher names to prove a topic. Check immediate fallback, automatically continuing bounded discovery, quoted evidence, user edits, cancellation and separate Ready/request outcomes. Recognized unsupported languages retain metadata fallback. Translation is not implemented or benchmarked. [Source reproduction and future performance gate](MULTILINGUAL-GROUPING.md)

## Scenario matrix

All real scenarios below are **pending because of the policy block**. Existing unit and mocked-preview results are separate evidence. Record actual times, observed states, fixture IDs and outcomes in a new run record; never prefill success.

| Phase                       | Scenario                       | Actions and acceptance criteria                                                                                                                                                                                                                                                        |
| --------------------------- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Gate                        | Tool access and version        | Establish a permitted extension-UI route; verify actual loaded version. Record only relevant fixture IDs, windows and model metadata.                                                                                                                                                  |
| Gate                        | Reset scope and no-model state | Inspect the supported uninstall control and scope; remove only the authorized model if the complete test can proceed. Verify actual downloadable/no-model state. Preserve Tabosmart history and user data.                                                                             |
| Main, uninterrupted         | Setup initiation               | Verify the fresh installation is already observing after Chrome permission grant and the nonblocking disclosure is visible. Use the explicit Settings model setup action when needed. Record real progress. Names and reasons must appear immediately without AI.                      |
| Main, uninterrupted         | Burst of 64 tabs               | Open prepared fixtures in two dedicated windows. No actions on personal tabs. Suggestions should settle automatically after bursts, show all valid proposals in actionable batches without a display cap, and show honest checking/cached/error feedback.                              |
| Main, uninterrupted         | Navigate/open/close burst      | Navigate selected fixture tabs among prepared URLs, open copies, close fixture copies, then allow a settled check. No duplicate setup sessions or app-induced cancellation.                                                                                                            |
| Main, uninterrupted         | Group/review while downloading | Open and cancel reviews, change group names, create groups, ungroup fixture groups, save fixtures and review duplicates. Download continues through dialog closure and successful actions.                                                                                             |
| Main, uninterrupted         | Protection and exact URLs      | Apply fixture protection/pin/audio states; keep one fixture active. Check safe exclusions, retained duplicate copy, and query/fragment distinctions. Close only explicitly selected fixtures after confirmation.                                                                       |
| Main, uninterrupted         | Worker suspension              | Leave the extension worker alone so Chrome may suspend it normally. Do not force-stop or inspect it in a way that keeps it alive. If suspension cannot be evidenced, record that limit. UI-managed setup must continue.                                                                |
| Main, uninterrupted         | Download completion            | Keep the initiating workspace open and do not toggle AI, sleep/restart Chrome, disconnect network, or impose an outer timeout. Real advancing progress may take longer than ten minutes overall. At 100%, preparation remains distinct until create resolves.                          |
| Main, uninterrupted         | Successful real inference      | After Ready, observe automatic names on at least two eligible visible fixture proposals, then test Rename. Obtain actual successful request evidence. Names must use real metadata, precede optional wording and preserve edited/open reviews. Readiness alone is not inference proof. |
| Main, uninterrupted         | Ready remains accurate         | Continue ordinary work beyond the former watchdog interval. A failed individual request keeps deterministic output and reports a request outcome; it must not masquerade as lost model/download failure. Detect actual availability loss if it occurs.                                 |
| Separate, after main passes | Reopen workspace               | Close and reopen the workspace, then verify current availability and persisted preference. If tested during a later download, label it an owner-page closure/reopen scenario; observe actual availability without assuming download cancellation.                                      |
| Separate, after main passes | Voluntary opt-out/cancel/retry | During a separate request turn AI off. Verify prompt/session cleanup, no late text or state replacement, truthful Off status, and a clean explicit retry.                                                                                                                              |
| Separate, after main passes | Cold start/restart             | Restart only an isolated authorized test browser instance. Do not quit the user's personal browser with unrelated state open. Verify durable URL context, fresh tab identity, startup grace and no false continuous-age claim. If isolation is unavailable, leave pending.             |
| Separate, after main passes | Deliberate interruption        | In an isolated test context, test network interruption, setup cancellation and resume/retry one at a time. Record what Chrome actually resumes or downloads again. Never interrupt the primary success run to manufacture coverage.                                                    |
| Cleanup                     | Fixture-only restoration       | Remove fixture groups/tabs that the run created; stop the new fixture server. Restore only test changes. Preserve the earlier three Garden studio fixtures until their separate manual test is complete.                                                                               |

## Visible work during the future run

Record actual queued, checking and completed states, with the summary's completed timestamp. Compare web-tab count, eligible review copies and visible cards against the fixture. Do not add overlapping exclusion categories. A failed check retains a clearly labeled earlier result; it does not become a fresh empty success.

Use the no-results scenarios for already grouped tabs, guarded tabs and insufficient age/grace, and confirm a useful next step remains available. Check the same views with reduced motion. Verify action confirmations against actual native groups and shelf entries, including partial failures. These checks establish behavior and legibility, not a measured emotional effect. The separate roughly 100-tab synthetic source preview does not replace this real 64-fixture run.

## Evidence already available

- `qa/first-run-fixture-checks.json`: 64 static HTTP fixtures validated without a browser.
- `qa/first-run-attempt.json`: the policy/reset boundary and user observations for this request.
- `qa/final-source-checks.json`: 247 passing 1.3.0 unit tests, passed source/scope checks and zero audit vulnerabilities. The archived 1.1.0 report keeps its historical 130-test evidence.
- `qa/preview-integration-report.json`: 134 passing 1.3.0 checks across 39 scenarios, with ten axe surfaces and no violations, page errors or external requests. Browser/history/model inputs are mocked.
- `qa/history-storage-report.json`: 25 passing native IndexedDB checks using 257 synthetic URLs and 771 visits across three reloads. Rollback, resume and erase use actual IndexedDB; history inputs are synthetic. No installed extension or model is accessed.
- `marketing/output/asset-manifest.json`: seven verified 1.3.0 source captures; zero axe violations, page errors or external requests. These and the labeled ten-state toolbar mock are not this real no-model run.

No model removal, fresh download, ready-to-inference transition, browser restart or real stress result has been verified by this blocked request. Earlier source tests and mocked previews do not validate 1.3.0 in installed Chrome. Verified source/capture results are separate from dist/tabosmart-1.3.0-chrome.zip and dist/tabosmart-1.3.0-source.zip archive records. Consult dist/package-report.json and dist/source-report.json for actual contents and hashes after packaging. None of those local results completes the real run.
